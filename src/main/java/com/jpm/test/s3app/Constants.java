package com.jpm.test.s3app;

import java.math.MathContext;
import java.math.RoundingMode;

public class Constants
{
    public static MathContext GLOBAL_MATH_CONTEXT = new MathContext(3, RoundingMode.HALF_UP);
}
